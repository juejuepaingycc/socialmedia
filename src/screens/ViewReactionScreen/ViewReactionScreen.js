import React, { useEffect, useContext, useState, useRef } from 'react';
import { Platform, TouchableOpacity, NativeModules, ScrollView, Modal, View, Share, Alert, 
    BackHandler, Image, Text, ToastAndroid } from 'react-native';
import { useSelector, ReactReduxContext } from 'react-redux';

const ViewReactionScreen = (props) => {
    return(
        <View>
            <Text>hhhhhhhhhhh</Text>
        </View>
    )
}

export default ViewReactionScreen;